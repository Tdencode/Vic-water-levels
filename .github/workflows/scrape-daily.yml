name: Daily scrape

on:
  schedule:
    # 20:00 UTC = 06:00 AEST (UTC+10) / 07:00 AEDT (UTC+11). Late enough
    # that overnight publishes have landed; (storage_id, reading_date)
    # uniqueness makes a re-run on the same day a no-op.
    - cron: "0 20 * * *"
  workflow_dispatch:
    inputs:
      company:
        description: "Optional company slug to run a single adapter"
        required: false
        type: string
      dry_run:
        description: "Print readings without writing to Supabase"
        required: false
        type: boolean
        default: false

# Don't pile up runs if a manual trigger lands while the scheduled one is
# still going. Daily readings are idempotent so cancelling is safe.
concurrency:
  group: daily-scrape
  cancel-in-progress: false

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install scrapers
        working-directory: scrapers
        run: pip install -e .

      - name: Run scrapers
        working-directory: scrapers
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        # Inline both flags as GitHub expressions: empty when the input is
        # blank/false, fully formed when set. Avoids fragile in-bash
        # conditionals around ${{ ... }} substitution.
        run: >-
          python -m scrapers.runner
          ${{ inputs.company && format('--company {0}', inputs.company) || '' }}
          ${{ inputs.dry_run && '--dry-run' || '' }}
